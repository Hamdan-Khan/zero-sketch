{
  "name": "@zero-sketch/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync-routes": "cargo test --manifest-path ../../services/api/Cargo.toml export_routes_to_worker",
    "predev": "pnpm sync-routes",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
